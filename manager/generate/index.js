const generate = require('./problem')
const generateList = require('./generate-org')
const generateMarkDown = require('./generate-md')

module.exports = {
  generate,
  generateList,
  generateMarkDown,
}